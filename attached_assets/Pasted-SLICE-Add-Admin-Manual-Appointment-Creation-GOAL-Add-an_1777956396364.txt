SLICE: Add Admin Manual Appointment Creation

GOAL:
Add an admin-only “Add Appointment” feature so HandyTech can manually enter appointments from the admin dashboard.

STRICT BOUNDARIES:
- Do NOT refactor the whole app.
- Do NOT rename files.
- Do NOT change public booking flow.
- Do NOT change admin login/auth system.
- Do NOT change existing database table names.
- Do NOT remove existing appointment features.
- Do NOT change deployment, ports, or environment variable behavior.
- Use existing styling/components where possible.
- Keep this slice focused only on manual admin appointment creation.

TASK:
In the admin dashboard schedule/appointments area:

1. Add a visible button:
“Add Appointment”

2. When clicked, open a modal or form with these fields:
- Customer name
- Phone
- Email
- Address
- Service
- Appointment date
- Appointment time
- Notes
- Status

3. Status options should include:
- New
- Confirmed
- Scheduled
- Completed
- Cancelled

4. Backend:
Create a protected admin route:

POST /api/admin/appointments

Requirements:
- Must require admin authentication.
- Must validate required fields.
- Must create or link a customer record using existing customers table.
- Must create an appointment using existing appointments table.
- Must return the created appointment or success response.

5. Frontend behavior:
- Submit form to POST /api/admin/appointments.
- Show loading state while saving.
- Show success message after saving.
- Show error message if save fails.
- Close modal after successful save.
- Refresh appointments/schedule list after successful save.

VERIFY:
After completing this slice, confirm:
- Admin login still works.
- Existing appointments still load.
- Public booking still works.
- Manual Add Appointment button appears in admin.
- Manual appointment saves successfully.
- New appointment still appears after page refresh.
- No console errors.
- No TypeScript/build errors.

DO NOT continue into unrelated features.
DO NOT redesign the homepage in this slice.
DO NOT add multi-tenant/business-template features.