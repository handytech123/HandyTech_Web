Awesome—let’s wire up Option 2 (direct SMS + email from your server) in a clean, production-ready way for your Node 20 app (works on Replit and your IONOS VPS/Plesk).

I’ll give you a tiny, drop-in /handoff API with:

input validation

duplicate-alert debouncing (per conversation)

Twilio SMS + SMTP email

rate limiting + security headers

clear env vars

copy-paste tests



---

Folder structure

your-app/
  package.json
  .env            # (local dev only)
  src/
    server.js
    routes/
      handoff.js
    lib/
      mailer.js
      sms.js
      validators.js


---

1) package.json (add deps)

{
  "name": "handytech-handoff",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node src/server.js",
    "start": "NODE_ENV=production node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "nodemailer": "^6.9.13",
    "twilio": "^5.3.5",
    "zod": "^3.23.8"
  }
}

> On IONOS, you’ll set env vars in Plesk (no .env needed). On Replit, keep .env and don’t commit it.




---

2) src/lib/validators.js

import { z } from "zod";

export const handoffSchema = z.object({
  customer_name: z.string().min(1).max(120).optional(),
  customer_email: z.string().email().optional(),
  customer_phone: z.string().min(7).max(32).optional(),
  channel: z.string().min(1).max(60).optional(),
  message: z.string().min(1).max(1000),
  page_url: z.string().url().optional(),
  conversation_url: z.string().url().optional(),
  conversation_id: z.string().min(3).max(120).optional(), // use this to de-dupe
  timestamp: z.string().datetime().optional()
});


---

3) src/lib/sms.js

import twilio from "twilio";

const sid = process.env.TWILIO_SID;
const token = process.env.TWILIO_TOKEN;
const from = process.env.TWILIO_FROM;     // e.g. +13145551234
const to = process.env.ALERT_TO_SMS;      // your cell

const client = sid && token ? twilio(sid, token) : null;

export async function sendSMS(body) {
  if (!client || !from || !to) {
    console.warn("[SMS] Skipped (missing TWILIO_* or ALERT_TO_SMS)");
    return { skipped: true };
  }
  const msg = await client.messages.create({ to, from, body });
  return { sid: msg.sid };
}


---

4) src/lib/mailer.js

import nodemailer from "nodemailer";

const host = process.env.SMTP_HOST;      // e.g. smtp.sendgrid.net
const port = process.env.SMTP_PORT || 587;
const user = process.env.SMTP_USER;
const pass = process.env.SMTP_PASS;
const from = process.env.FROM_EMAIL;     // e.g. alerts@handytech-solutions.com
const to   = process.env.ALERT_TO_EMAIL; // your inbox

let transporter = null;
if (host && user && pass) {
  transporter = nodemailer.createTransport({
    host,
    port: Number(port),
    secure: Number(port) === 465,
    auth: { user, pass }
  });
}

export async function sendEmail({ subject, text, html }) {
  if (!transporter || !from || !to) {
    console.warn("[Email] Skipped (missing SMTP_* or FROM/TO)");
    return { skipped: true };
  }
  const info = await transporter.sendMail({ from, to, subject, text, html });
  return { messageId: info.messageId };
}


---

5) src/routes/handoff.js

import express from "express";
import { handoffSchema } from "../lib/validators.js";
import { sendSMS } from "../lib/sms.js";
import { sendEmail } from "../lib/mailer.js";

/** In-memory dedupe window to prevent spammy repeats */
const DEDUPE_WINDOW_MS = 5 * 60 * 1000; // 5 minutes
const recentAlerts = new Map(); // key: conversation_id, val: lastTs

const router = express.Router();

router.post("/", async (req, res) => {
  try {
    const parsed = handoffSchema.parse(req.body || {});
    const {
      customer_name, customer_email, customer_phone,
      channel, message, page_url, conversation_url, conversation_id
    } = parsed;

    // De-dupe by conversation_id (prefer) or by message hash fallback
    const now = Date.now();
    const dedupeKey = conversation_id || `${(message || "").slice(0,80)}|${customer_phone || ""}`;
    const last = recentAlerts.get(dedupeKey);
    if (last && now - last < DEDUPE_WINDOW_MS) {
      return res.status(200).json({ ok: true, deduped: true });
    }
    recentAlerts.set(dedupeKey, now);

    // Build plain text & SMS bodies
    const short = `LIVE CHAT${channel ? " (" + channel + ")" : ""}: ${customer_name || "Visitor"} — ${message}${page_url ? " | " + page_url : ""}`;

    const text = [
      `⚡ Live Chat Request ${channel ? `(${channel})` : ""}`,
      `Name:  ${customer_name || "Unknown"}`,
      `Email: ${customer_email || "Unknown"}`,
      `Phone: ${customer_phone || "Unknown"}`,
      `Message: ${message}`,
      page_url ? `Page: ${page_url}` : null,
      conversation_url ? `Transcript: ${conversation_url}` : null,
      conversation_id ? `Conversation ID: ${conversation_id}` : null,
      `Time: ${new Date().toISOString()}`
    ].filter(Boolean).join("\n");

    // Fire both channels in parallel (don’t fail all if one fails)
    const [smsResult, mailResult] = await Promise.allSettled([
      sendSMS(short),
      sendEmail({
        subject: "⚡ Live Chat Request",
        text,
        html: text.replace(/\n/g, "<br>")
      })
    ]);

    const details = {
      sms: smsResult.status === "fulfilled" ? smsResult.value : { error: smsResult.reason?.message },
      email: mailResult.status === "fulfilled" ? mailResult.value : { error: mailResult.reason?.message }
    };

    return res.status(200).json({ ok: true, ...details });
  } catch (err) {
    // zod validation or runtime errors
    const msg = err?.errors ? "invalid_payload" : "handoff_failed";
    if (err?.errors) {
      return res.status(400).json({ ok: false, error: msg, details: err.errors });
    }
    console.error("[/handoff]", err);
    return res.status(500).json({ ok: false, error: msg });
  }
});

export default router;


---

6) src/server.js

import "dotenv/config";        // no-op on IONOS (no .env), fine locally
import express from "express";
import helmet from "helmet";
import cors from "cors";
import morgan from "morgan";
import rateLimit from "express-rate-limit";
import handoffRouter from "./routes/handoff.js";

const app = express();

// If behind Plesk/nginx or Replit proxy:
app.set("trust proxy", 1);

app.use(helmet());
app.use(cors({
  origin: process.env.CORS_ORIGIN?.split(",") || "*"
}));
app.use(express.json({ limit: "512kb" }));
app.use(morgan(process.env.NODE_ENV === "production" ? "combined" : "dev"));

const limiter = rateLimit({
  windowMs: 60 * 1000,
  max: 30,               // 30 requests/min per IP
  standardHeaders: true,
  legacyHeaders: false
});
app.use("/handoff", limiter, handoffRouter);

// (Optional) Health check
app.get("/healthz", (_req, res) => res.json({ ok: true }));

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`Server listening on :${PORT}`);
});


---

7) Environment variables

Set these in Plesk → Node.js app → Environment Variables (IONOS), or in Replit Secrets:

# Twilio (SMS)
TWILIO_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_FROM=+1314xxxxxxx
ALERT_TO_SMS=+1314yyyyyyy

# SMTP (email) – SendGrid, Postmark, or your provider
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey           # (SendGrid convention)
SMTP_PASS=SG.xxxxxx
FROM_EMAIL=alerts@handytech-solutions.com
ALERT_TO_EMAIL=luis@yourdomain.com

# CORS (optional, comma-separated)
CORS_ORIGIN=https://handytech-solutions.com,https://your-replit-domain.repl.co

> You can start with just SMS or just Email; if any set is missing, that channel is skipped (and logged).




---

8) Hook your bot to the endpoint

Wherever your chatbot determines human handoff:

// Inside your bot’s handler
await fetch(process.env.HANDOFF_URL || "/handoff", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    customer_name: session.user?.name,
    customer_email: session.user?.email,
    customer_phone: session.user?.phone,
    channel: "website widget",
    message: userMessage,                      // last message or summary
    page_url: typeof window !== "undefined" ? window.location.href : undefined,
    conversation_url: session.transcriptUrl,   // if you have one
    conversation_id: session.id                // use your convo id for de-dupe
  })
});
// Then reply in chat:
return "Got it — I’ve alerted Luis to join this chat. What’s the best number/email if he needs to reach you?";

If your front end is on a different origin, call your public API URL, e.g. https://api.handytech-solutions.com/handoff.


---

9) Quick tests

Curl (SMS+Email):

curl -X POST http://localhost:3000/handoff \
  -H "Content-Type: application/json" \
  -d '{
    "customer_name": "Jane Doe",
    "customer_email": "jane@example.com",
    "customer_phone": "+13145551234",
    "channel": "website",
    "message": "I need help with a leaky faucet",
    "page_url": "https://handytech-solutions.com/booking",
    "conversation_url": "https://chat.example.com/t/abc123",
    "conversation_id": "abc123"
  }'

De-dupe check (run again within 5 min; response contains deduped: true).

Health check:

curl http://localhost:3000/healthz


---

10) Production notes (IONOS/Plesk)

In Plesk → Node.js, pick Node 20.x and set:

Application startup file: src/server.js

Document root can stay default; API listens on the port Plesk assigns.


If you later add nginx reverse-proxy, keep app.set('trust proxy', 1) as shown.

If your site is SSR or you have a separate front end, just ensure CORS origins include your site domain(s).



---

11) Optional nice-to-haves

Multiple recipients: set ALERT_TO_SMS to a comma-separated list and loop-send.

Business hours: add logic to change the SMS text after hours (e.g., “after-hours request”).

Throttling per user: extend dedupe key with phone/email.

Slack ping: add a third Promise to post to Slack Incoming Webhook.



---

If you paste these files in as-is, set your env vars, and hit /handoff, you’ll get instant pings. Want me to tailor the SMTP for your current provider or add Slack as a third channel?

