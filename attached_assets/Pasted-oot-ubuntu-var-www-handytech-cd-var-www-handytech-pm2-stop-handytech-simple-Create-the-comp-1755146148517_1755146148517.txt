oot@ubuntu:/var/www/handytech# cd /var/www/handytech
pm2 stop handytech-simple

# Create the complete server with all features
cat > server-complete.mjs << 'EOF'
import express from 'express';
import { fileURLToPath } from 'url';
import { dirname, join } from 'path';
import pkg from 'pg';
const { Pool } = pkg;

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

const app = express();
const PORT = 3000;

// Database connection
const pool = new Pool({
  user: 'handytech_user',
  host: 'localhost',
  database: 'handytech_db',
  password: process.env.DB_PASSWORD || 'your_password_here',
  port: 5432,
});

app.use(express.json());
app.use(express.static(join(__dirname, 'client/dist')));
app.use(express.static(join(__dirname, 'public')));

// Serve the main React app
app.get('/', (req, res) => {
  res.sendFile(join(__dirname, 'client/index.html'));
});

// Admin login endpoint
app.post('/api/admin/login', (req, res) => {
  const { username, password } = req.body;
  if (username === 'admin' && password === 'HandyTech2024!') {
    res.json({ success: true, message: 'Login successful' });
  } else {
    res.status(401).json({ success: false, message: 'Invalid credentials' });
  }
});

// API endpoints for business features
app.get('/api/customers', async (req, res) => {
  try {
    const result = await pool.query('SELECT * FROM customers ORDER BY created_at DESC');
    res.json(result.rows);
  } catch (error) {
    res.status(500).json({ error: 'Database error' });
  }
});

app.get('/api/appointments', async (req, res) => {
  try {
    const result = await pool.query('SELECT * FROM appointments ORDER BY scheduled_date DESC');
    res.json(result.rows);
  } catch (error) {
    res.status(500).json({ error: 'Database error' });
  }
});

app.get('/api/quotes', async (req, res) => {
  try {
    const result = await pool.query('SELECT * FROM quotes ORDER BY created_at DESC');
    res.json(result.rows);
  } catch (error) {
    res.status(500).json({ error: 'Database error' });
pm2 logs handytech-complete --lines 5handytech-completeonnections`); ${PORT}`);
[PM2] Applying action stopProcessId on app [handytech-simple](ids: [ 3 ])
[PM2] [handytech-simple](3) ✓
┌────┬───────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┬──────────┬──────────┬──────────┬──────────┐
│ id │ name                  │ namespace   │ version │ mode    │ pid      │ uptime │ ↺    │ status    │ cpu      │ mem      │ user     │ watching │
├────┼───────────────────────┼─────────────┼─────────┼─────────┼──────────┼────────┼──────┼───────────┼──────────┼──────────┼──────────┼──────────┤
│ 2  │ handytech             │ default     │ 1.0.0   │ fork    │ 0        │ 0      │ 0    │ stopped   │ 0%       │ 0b       │ root     │ disabled │
│ 4  │ handytech-complete    │ default     │ 1.0.0   │ fork    │ 21196    │ 38s    │ 0    │ online    │ 0%       │ 67.9mb   │ root     │ disabled │
│ 3  │ handytech-simple      │ default     │ 1.0.0   │ fork    │ 0        │ 0      │ 0    │ stopped   │ 0%       │ 0b       │ root     │ disabled │
└────┴───────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘
[PM2][ERROR] Script already launched, add -f option to force re-execution
[PM2] Saving current process list...
[PM2] Successfully saved in /root/.pm2/dump.pm2
[TAILING] Tailing last 5 lines for [handytech-complete] process (change the value with --lines option)
/root/.pm2/logs/handytech-complete-error.log last 5 lines:
/root/.pm2/logs/handytech-complete-out.log last 5 lines:
4|handytec | HandyTech Solutions complete platform running on port 3000
4|handytec | Database connected: 0 connections