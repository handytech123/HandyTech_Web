SLICE: Fix Book Service Button Flow

GOAL:
Change the customer-facing “Book Service” buttons so they open/go directly to the scheduling/booking form, not the estimate/request quote section.

STRICT BOUNDARIES:
- Do NOT refactor the whole app.
- Do NOT change backend routes.
- Do NOT change database schema.
- Do NOT change admin dashboard.
- Do NOT change authentication.
- Do NOT remove the estimate/request quote form.
- Do NOT break existing appointment creation.
- Keep this slice focused only on button behavior and booking flow clarity.

TASK:
Update the public/customer-facing site behavior:

1. “Book Service Now” buttons should:
- Open the existing scheduling/appointment booking form directly
OR
- Scroll directly to the scheduling/appointment booking section if it already exists.

2. Service card “Book Service” buttons should:
- Preselect or pass the selected service if existing logic supports it.
- Then open/scroll to the scheduling form.

3. “Book Tech Service” button should:
- Open/scroll to scheduling form.
- Preferably preselect a tech-related service if supported.

4. Estimate/request quote should remain available, but secondary.
Rename or position it as:
“Need a custom quote?”
or
“Request Estimate”

5. Update button labels if needed:
Primary:
“Book Service Now”
“Schedule Appointment”

Secondary:
“Request Estimate”
“Need a Custom Quote?”

6. Make sure customer flow is:
Homepage → Book Service → Choose service/date/time → Submit appointment

VERIFY:
- Clicking hero “Book Service Now” opens/goes to appointment booking, not estimate.
- Clicking service card “Book Service” opens/goes to appointment booking.
- Estimate/request quote still exists and works separately.
- Public booking still creates appointments.
- Admin still shows appointments.
- No console errors.
- No TypeScript/build errors.

DO NOT add unrelated homepage redesign work.
DO NOT add SMS changes in this slice.
DO NOT add new database fields.